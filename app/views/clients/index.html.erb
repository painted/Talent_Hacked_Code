  <h3>List of clients</h3>
<% if @clients.any? %>
  <% @clients.each do |client| %>
	<p><%= link_to "#{client.name}", edit_client_registration_path(client.id) %>
    <%= link_to 'Delete', edit_client_registration_path(client.id), method: :delete, data: { confirm: 'Are you sure?' } %></p>
  <% end %>
<% else %>
  No clients
<% end %>

<p><%= link_to "Add clients", new_client_path %></p>